@model SoundBarrierHunting.Models.CartIndexViewModel
 
@{
    ViewBag.Title = "Cart";
    Layout = "~/Views/Shared/Layouts/_Layout.cshtml";
}

<div id="ProductContainer">
    <div class="text-center" id="CartTitle">My Cart</div>

    <table class="table">
        <thead>
            <tr>
                <th>Quantity</th>
                <th>Item</th>
                <th class="text-right">Price</th>
                <th class="text-right">Subtotal</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var line in Model.Cart.Lines)
            {
                <tr>
                    <td class="text-center">@line.Quantity</td>
                    <td class="text-left">@line.Product.Name</td>
                    <td class="text-right">@line.Product.Price.ToString("c")</td>
                    <td class="text-right">@((line.Quantity*line.Product.Price).ToString("c"))</td>                
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3" class="text-right">Total:</td>
                <td class="text-right">@Model.Cart.ComputeTotalValue().ToString("c")</td>
            </tr>
        </tfoot>
    </table>

    <div class="text-center">
        <a class="btn btn-primary" href="@Model.ReturnUrl">Continue Shopping</a>
    </div>
    <div class="text-right" id="CheckOutButton">
        <a href="@Url.Action()">Check Out</a>
    </div>

    @*<div class="container" id="SoundAdviceContainer">
        <div class="col-lg-3">
            <p id="VideoDescription">Sound Barrier Unit</p>
        </div>
        <div class="col-lg-3">
            <p class="text-center" id="VideoDescription">
                Shipping Info
            </p>
        </div>
        <div class="col-lg-3">
            <p class="text-center" id="VideoDescription">
                # of Item: 1
            </p>
        </div>
        <div class="col-lg-3">
            <p class="text-center" id="VideoDescription">
                $199.99
            </p>
        </div>
    </div>

    <div class="container" id="SoundAdviceContainer">
        <div class="col-lg-3">
            <p id="VideoDescription">Sound Barrier T-Shirt</p>
        </div>
        <div class="col-lg-3">
            <p class="text-center" id="VideoDescription">
                Shipping Info
            </p>
        </div>
        <div class="col-lg-3">
            <p class="text-center" id="VideoDescription">
                # of Item: 1
            </p>
        </div>
        <div class="col-lg-3">
            <p class="text-center" id="VideoDescription">
                $17.99
            </p>
        </div>
     </div>
    <div class="text-right" id="CheckOutButton">Total: $219.06</div>*@
</div>